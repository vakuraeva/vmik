# Программы task2

### 2_1.c
Вычисление квадратного корня числа методом Ньютона с заданной epsilon точностью.
В консоль воодится epsilon, далле пока не конец считываются числа и выводится их квадратный корень.

### 2_2.c  
task 2.2.2 + task 2.2.2
Вводится последовательность чисел x, An, ..., A1, A0, разделенных пробелом. Конец файла - признак конца последовательности. Далее программа вычисляет значение многочлена по схеме горнера с заданными коэффициентами и х. Также программа выводит значение производной заданного многочлена.

### 2_3.c
Сравнение скорости вывода заданного с клавиатуры элемента  последовательности Фибоначчи с помощью рекурсивной и итеративной функции. В программе описана функция fib_recursive(), работающая рекурсивно, и fib_iterative(), работающая итеративно. В ходе эксперимента было выясненно, что вторая функция работает значительно быстрее первой. 

### 2_4.c
Преобразует строку в вещественное число типа double. Также добавлена дополнительная функция: при использовании М вместо Е, выводится максимальная цифра в записи числа.

### 2_5.c
Из заданной строки создается список, как цепочка динамическич объектов. Из списка исключаются все слова, совпадающие с последним. Функции используемые в программе:
read_input_string() - создает динамический список
print_list() - выводит список в консоль
modify_list() - модиффикация списка


