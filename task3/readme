программа реализует следующие задачи:
1) cчитывает из stdin очередную строку,
2) преобразует ее в последовательность (список) слов,
3) выводит в stdout полученную последовательность слов в исходном порядке,
4) выводит слова последовательности в лексикографическом порядке.

При выводе в первой строке печатается длина списка, в каждой последующей - очередной элемент списка.

Программа состоит из 3 модулей: 
 
Модуль main.c:

    Главная функция программы - читает строки из stdin пока не достигнет конца файла

    Инициализирует список слов и обрабатывает каждую строку

    Выводит результаты: сначала количество слов, затем слова в исходном порядке, потом отсортированные слова

    Очищает список после обработки каждой строки

Модуль wordlist.h:

    Заголовочный файл с объявлениями структур и функций

    Определяет структуру WordList для хранения массива слов

    Объявляет функции для работы со списком слов

Модуль wordlist.c:

    init_list() - инициализирует пустой список с начальной емкостью

    add_word() - добавляет слово в список, увеличивая емкость при необходимости

    sort_list() - сортирует слова пузырьковой сортировкой

    clear_list() - освобождает память и очищает список

    process_line() - разбирает строку на слова, разделяя простые и специальные слова
